const galleryReady = () => {
	console.log('gallery is ready.');

	const picturesEl = document.getElementsByClassName('pictures').item(0);
	// TODO download, share, etc.
};

document.addEventListener('DOMContentLoaded', galleryReady);
